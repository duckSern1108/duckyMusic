# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DuckyMusic' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
pod 'SDWebImage', '~> 5.0'
pod 'Alamofire'
pod "PromiseKit", "~> 6.8"
pod 'ObjectMapper', '~> 3.5'
pod 'ESTMusicIndicator'

  # Pods for DuckyMusic

  target 'DuckyMusicTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DuckyMusicUITests' do
    # Pods for testing
  end

end
